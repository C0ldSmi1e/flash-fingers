services:
  web:
    build: .
    image: flash-fingers
    container_name: flash-fingers
    restart: unless-stopped
    env_file: .env
    volumes:
      - ./data:/app/data
    networks:
      - tunnel-net

networks:
  tunnel-net:
    external: true
