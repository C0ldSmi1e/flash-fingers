export const pagination = {
  defaultLimit: 10,
  defaultOffset: 0,
  maxLimit: 1000,
};

export const contentText = {
  minLength: 20,
  maxLength: 150,
};

// Submissions above this are rejected as implausible.
export const record = {
  maxWpm: 3000,
};
